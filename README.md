# Student Scores Regression

This project performs a linear regression analysis on student scores based on study hours.

## Files
- `student_scores.csv`: dataset containing study hours and exam scores
- `regression.py`: Python script to run regression and plot results

## How to Run
1. Install required libraries:
   ```bash
   pip install pandas scikit-learn matplotlib
